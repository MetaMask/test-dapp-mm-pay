import React from 'react';
import ReactDOM from 'react-dom/client';

import { AppMmPay } from './AppMmPay.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppMmPay />
  </React.StrictMode>,
);
